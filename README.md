# apktojs

Rust library used by [hoshi](https://github.com/dot-fx/hoshi) to translate extension based APKs into JavaScript code.

The generated output is intended for compatibility/runtime use inside hoshi and is not meant to produce clean or human-written JavaScript.